apiVersion: apps/v1
kind: Deployment
metadata:
  name: pcs-poc
  namespace: pcs-poc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pcs-poc
      version: v1.0
  template:
    metadata:
      labels:
        app: pcs-poc
        version: v1.0
    spec:
      containers:
      - name: pcs-poc
        image: pcsregistrytest.azurecr.io/j2eedemo:v1.0
        imagePullPolicy: IfNotPresent
        ports:
          - containerPort: 8080

            
            
